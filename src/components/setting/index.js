import React from 'react';
import './setting.css'

const Setting = () => {
    return (
        <div className='container'>
            <h1>Settings...</h1>
        </div>
    );
};

export default Setting;